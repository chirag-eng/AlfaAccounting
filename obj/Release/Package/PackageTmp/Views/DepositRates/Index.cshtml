@model IEnumerable<AlfaAccounting.Models.DepositRate>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutLoggedIn.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DepositRateValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DepositRateValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DepositRateId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DepositRateId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DepositRateId })
        </td>
    </tr>
}

</table>
