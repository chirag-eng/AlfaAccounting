@model IEnumerable<AlfaAccounting.Models.UnitPrice>

@{
    ViewBag.Title = "Unit Price List";
    Layout = "~/Views/Shared/_LayoutLoggedIn.cshtml";
}

<h2>Unit Price</h2>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@
@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UnitPriceValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UnitPriceDescription)
        </th>
        <th></th>
    </tr>

    @using (Html.BeginForm("Edit", "UnitPrices", new { id = item.UnitPriceId }, FormMethod.Post))
            {
        @Html.AntiForgeryToken()

        for (int i = 0; i < Model.Count(); i++)
        {
            {
                <tr>
                    <td>
                        @Html.EditorFor(model => Model.ToList()[i].UnitPriceValue, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => Model.ToList()[i].UnitPriceValue, "", new { @class = "text-danger" })
                    </td>
                    <td>
                        @Html.EditorFor(model => Model.ToList()[i].UnitPriceDescription, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => Model.ToList()[i].UnitPriceDescription, "", new { @class = "text-danger" })
                    </td>
                    <td>
                        <input type="submit" value="update" class="btn btn-default" />
                        @Html.ActionLink("Edit", "Edit", new { id = item.UnitPriceId }, new { @class="btn btn-primary" })|
                        @Html.ActionLink("Delete", "Delete", new { id = Model.ToList()[i].UnitPriceId }, new { onclick = "return confirm('Are you sure you want to delete this record?');", @class = "btn btn-primary" })
                    </td>
                </tr>
            }
        }
    }
</table>*@

<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UnitPriceValue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UnitPriceDescription)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UnitPriceValue)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UnitPriceDescription)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.UnitPriceId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.UnitPriceId }, new { onclick = "return confirm('Are you sure you want to delete this record?');" })
                </td>
            </tr>
        }

    </table>

@using (Html.BeginForm("Index", "UnitPrices", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    @*<div class="form-horizontal">*@
    <h4>Add New Unit Price</h4>
    <hr />
    <h4 class="text-danger"> @ViewBag.Error</h4>

    <div class="row">
        <div class="form-group">
            @Html.Label("Unit Price", htmlAttributes: new { @class = "control-label col-md-3" })
            <div class="col-md-10">
                @Html.TextBox("UnitPriceValue", 0, new { htmlAttributes = new { @class = "form-control" } })
                @*@Html.TextBoxFor(model => item2.UnitPriceValue, new { htmlAttributes = new { @class = "form-control" } })*@
                @*@Html.ValidationMessageFor(model =>item2.UnitPriceValue, "Error", new { @class = "text-danger" })*@
            </div>
        </div>

        <div class="form-group">
            @Html.Label("UnitPriceDescription", htmlAttributes: new { @class = "control-label col-md-3" })
            <div class="col-md-10">
                @Html.TextBox("UnitPriceDescription", null, new { htmlAttributes = new { @class = "form-control" } })
                @*@Html.TextBoxFor(model => item2.UnitPriceDescription, new { htmlAttributes = new { @class = "form-control" } })*@
                @*@Html.ValidationMessageFor(model => item2.UnitPriceValue, "Error", new { @class = "text-danger" })*@
            </div>
        </div>
        
        <div class="form-group">
            <div class="col-md-offset-1 col-md-11">
                <input type="submit" value="Create" class="btn btn-default" />
                <hidden></hidden>
            </div>
        </div>
    </div>
       @*</div>*@
}
